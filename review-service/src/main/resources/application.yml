server:
  port: 7003
  error:
    include-message: always

logging:
  level:
    root: info
    org.emangini.servolution: debug

---
spring:
  config:
    activate:
      on-profile: docker

server:
  port: 8080
